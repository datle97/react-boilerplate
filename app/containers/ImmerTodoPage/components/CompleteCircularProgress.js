import styled from 'styled-components';
import StyledCircularProgress from './StyledCircularProgress';

const CompleteCircularProgress = styled(StyledCircularProgress)`
  margin-top: -24px;
  margin-left: -24px;
`;

export default CompleteCircularProgress;
